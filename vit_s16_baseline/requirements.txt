# ------------------------------------------------------------
# Dependencies for the ViT-S/16 baseline project.
#
# On Google Colab, `torch` and `torchvision` are ALREADY installed
# (with the correct CUDA build), so do NOT reinstall them there or
# you may break the GPU runtime. The remaining packages are light
# and usually present; install them with:
#
#     pip install -r requirements.txt
#
# (torch/torchvision lines are commented out for Colab safety.)
# ------------------------------------------------------------

# torch>=2.0          # preinstalled on Colab
# torchvision>=0.15   # preinstalled on Colab

numpy>=1.24
pyyaml>=6.0
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0
tqdm>=4.66
Pillow>=9.5
