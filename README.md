# room-classification
Transformer-based 6-class room classification (bathroom, kitchen, living, bedroom, hall, dining) on a public Places365 RGB subset (256x256). Baseline ViT + Center Loss for separable embeddings + online pseudo-labeling (semi-supervised). Implemented in Python/PyTorch with hyperparameter sweeps. Eval: accuracy, confusion matrices, embedding plots
